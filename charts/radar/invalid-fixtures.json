[
  {
    "description": "series is missing",
    "spec": {"type": "radar"},
    "errors": ["$.series: required"]
  },
  {
    "description": "series is not an array",
    "spec": {"type": "radar", "series": "bad"},
    "errors": ["$.series: expected array, received string"]
  },
  {
    "description": "series element is not an object",
    "spec": {"type": "radar", "series": [42]},
    "errors": ["$.series[0]: expected object, received number"]
  },
  {
    "description": "series element missing data",
    "spec": {"type": "radar", "series": [{"name": "A"}]},
    "errors": ["$.series[0].data: required"]
  },
  {
    "description": "series data is not an array",
    "spec": {"type": "radar", "series": [{"data": "bad"}]},
    "errors": ["$.series[0].data: expected array, received string"]
  },
  {
    "description": "series data element is not a number",
    "spec": {"type": "radar", "series": [{"data": ["oops"]}]},
    "errors": ["$.series[0].data[0]: expected number, received string"]
  },
  {
    "description": "xAxis.categories is not an array",
    "spec": {"type": "radar", "series": [{"data": [1,2,3]}], "xAxis": {"categories": 42}},
    "errors": ["$.xAxis.categories: expected array, received number"]
  },
  {
    "description": "xAxis.categories element is not a string",
    "spec": {"type": "radar", "series": [{"data": [1,2,3]}], "xAxis": {"categories": [1]}},
    "errors": ["$.xAxis.categories[0]: expected string, received number"]
  },
  {
    "description": "fillOpacity is not a number",
    "spec": {"type": "radar", "series": [{"data": [1,2,3], "fillOpacity": "high"}]},
    "errors": ["$.series[0].fillOpacity: expected number, received string"]
  }
]
