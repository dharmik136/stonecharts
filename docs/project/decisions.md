---
id: SC-OPS-004
title: StoneCharts Decision Register
status: approved
classification: informative
owner: product-owner
approver: maintainer
review_mode: self
applies_to: 0.0.0.1 and later
requirements: []
evidence: [TEST-DOCS-CONTROL]
last_reviewed: "2026-07-27"
review_due: "2026-08-27"
supersedes: null
superseded_by: null
---

# Decision Register

Recommendations are starting positions, not approvals. Accepted architectural choices
receive an ADR; bounded operating decisions name their controlling project document.

## Resolved decisions

| ID | Decision | Resolution | Authority | Resolved |
|---|---|---|---|---|
| DEC-001 | Canonical first release identifier | Use exactly `0.0.0.1`; do not represent it as SemVer or publish invalid `v0.0.0.1` Go tags | [ADR 0007](../architecture/adr/0007-release-identifier.md) | 2026-07-18 |
| DEC-002 | Active 0.0.0.1 chart scope | Accept `line`, `column`, and `area` in the active release scope; keep the other chart designs informative until each has a renderer and conformance corpus | [Project operating model](README.md), [Stage 0 gate](stage-0.md), [Positioning and alpha scope](../product/positioning-and-scope.md) | 2026-07-20 |
| DEC-003 | Additional language renderer timing | Design the language conformance kit now, but defer new language implementation until Python and Go have proven the contract end to end through 0.0.0.1 qualification | [Renderer constitution](../architecture/renderer-constitution.md), [Stage 0 gate](stage-0.md) | 2026-07-18 |
| DEC-004 | Structured customization surface for 0.0.0.1 | Keep customization typed, deterministic, and schema-governed; allow themes, sizing, margins, palettes, series styling, gradients, patterns, and supported mark controls; exclude raw CSS, raw SVG, callbacks, DOM mutation, and other executable escape hatches from certified output | [Customization boundary](../contracts/customization-boundary.md), [ADR 0005](../architecture/adr/0005-layout-and-fonts.md) | 2026-07-18 |
| DEC-005 | Compatibility policy beginning with 0.0.0.1 | Allow governed pre-release breaks during qualification; after release, require migration notes, deprecation windows where feasible, traceability updates, and checklist evidence for public-surface changes | [Compatibility policy](../contracts/compatibility-policy.md), [Release plan](../releases/0.0.0.1/plan.md) | 2026-07-18 |
| DEC-006 | Default branch and merge strategy | Use `main` as the default branch and keep the authoritative merge policy recorded in the governed project documents | [Project operating model](README.md), [Stage 0 gate](stage-0.md) | 2026-07-18 |
| DEC-007 | GitHub Project fields and workflow | Use the schema-validated backlog, controlled status model, stable IDs, traceability fields, and local/remote conformance checks | [Project operating model](README.md), [Stage 0 gate](stage-0.md) | 2026-07-18 |
| DEC-008 | Supported runtime and platform matrix | Support Python 3.9 and 3.14, Go 1.26, Chromium on the pinned desktop Linux profile through local HTTP, and no certified exporter profile for 0.0.0.1 | [Runtime matrix](../contracts/runtime-matrix.md), [Visual profile](../contracts/visual-profile.md), [Release plan](../releases/0.0.0.1/plan.md) | 2026-07-18 |
| DEC-010 | Certified visual profile for 0.0.0.1 | Use the host-font semantic SVG profile as the certified visual baseline; reserve embedded-font and pinned-exporter profiles for later decisions | [Visual profile](../contracts/visual-profile.md), [Typography and export profiles](../contracts/typography-and-export-profiles.md) | 2026-07-18 |
| DEC-009 | Performance and artifact-size release budgets | Use a reproducible baseline across small, business, dense, and stress workloads, with regression budgets derived from observed variance rather than invented thresholds | [Performance budget policy](../contracts/performance-budgets.md), [Benchmark specification](../quality/benchmark-spec.md) | 2026-07-18 |
| DEC-011 | Package and source publication channels | Keep the repository and registries private until S3 evidence is complete; publish only through supportable channels recorded in the release plan and supply-chain policy | [Publication policy](../contracts/publication-policy.md), [Supply-chain policy](../security/supply-chain.md), [Release plan](../releases/0.0.0.1/plan.md) | 2026-07-18 |
| DEC-012 | StoneCharts name clearance for public commercial use | Require a dated due-diligence record before any public commercial name claim; technical namespace adoption is not legal clearance | [Name-clearance policy](../contracts/name-clearance-policy.md), [ADR 0006](../architecture/adr/0006-stonecharts-namespace.md) | 2026-07-18 |
| DEC-013 | Commercial license, contribution, support, and access model | Keep the product proprietary until a written business policy is approved; internal access does not imply external rights | [Commercial terms policy](../contracts/commercial-terms-policy.md), [Product positioning and alpha scope](../product/positioning-and-scope.md) | 2026-07-18 |
| DEC-014 | Post-0.0.0.1 expansion order | Admit `bar` as the next chart type, targeting release `0.0.0.2`; per the roadmap's own analysis it reuses column's band-layout, stacking, and all shared chrome via a pure orientation transpose, with no new data or point model. No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full - this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Stage 5 expansion gate](backlog.yaml) | 2026-07-26 |
| DEC-015 | Post-0.0.0.2 expansion order | Admit `scatter` as the next chart type, targeting release `0.0.0.3`; per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 3) it is the cheapest remaining Cartesian sibling, but unlike bar it forces two genuine generalizations shared across every chart type: the point model (`series[].data` element normalizes to `{x,y}` / `[x,y]` / a pinned bare-number fast path) and a numeric `linear` x-scale (reusing `nice_ticks` with `include_zero=False`). No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full, including the roadmap's mandatory byte-identity gate proving every existing golden (line/column/area/bar) is unchanged once the point model lands - this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Scatter design](../../charts/scatter/design.md) | 2026-07-27 |
| DEC-016 | Post-0.0.0.3 expansion order | Admit `bubble` as the next chart type, targeting release `0.0.0.4`; per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 4) it is the cheapest remaining Cartesian sibling, riding scatter's already-landed numeric linear x-scale and point model verbatim with exactly one net-new generalization: the size-scale (`z -> area-proportional radius`, `r ∝ sqrt(z)`, a pinned degenerate-domain rule). No shared-frame changes are required - unlike scatter, this is a marks-function-only admission comparable in shape to bar's. No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full - this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Bubble design](../../charts/bubble/design.md) | 2026-07-28 |
| DEC-017 | Post-0.0.0.4 product direction | Pause broad chart-family expansion after 0.0.0.4 and focus the next phase on Visual Integrity Infrastructure validation in insurance reporting plus a working StoneVerify conformance workflow; new chart types, languages, hosted services, and document-generation capabilities require paid customer evidence or explicit approval as necessary validation infrastructure | [Visual integrity strategy](../product/visual-integrity-strategy.md), [Visual integrity repositioning brief](decision-briefs/dec-017-visual-integrity.md) | 2026-07-28 |
| DEC-018 | Near-term licensing boundary for Visual Integrity validation | Keep the repository, current specification, fixtures, renderers, verification tooling, and pilot materials proprietary under the existing license. Public specification, open-core packaging, public fixture corpora, or community runtimes are deferred hypotheses and require a later explicit licensing and publication decision before they may be offered or marketed. | [Commercial terms policy](../contracts/commercial-terms-policy.md), [Capability matrix](../product/capability-matrix.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-07-28 |
| DEC-019 | Gated demo site for prospect discovery | Create a gated demo website at `site/` using golden SVG outputs, SC-QUAL-003 benchmark data, and StoneVerify evidence. No source code or packages distributed. The public landing page describes the problem space without product branding (DEC-012 compliant); the gated demo uses the full product name and is accessible only via Cloudflare Access email allow-list. | [Visual integrity strategy](../product/visual-integrity-strategy.md), [Publication policy](../contracts/publication-policy.md) | 2026-08-07 |
| DEC-020 | Post-0.0.0.4 expansion: combo as validation infrastructure | Admit `combo` as the next chart type, targeting release `0.0.0.5`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the composition layer (per-series `series[].type` mark dispatch) and the dual y-axis (`secondaryYAxis`) are required to validate the StoneVerify conformance workflow against the insurance reporting segment's standard "magnitude bars + trend line on dual axes" view. Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 6) combo is a composition sibling that reuses column's band-layout/stacking and line's path/markers verbatim, adding exactly two net-new generalizations: the composition-layer dispatch and the optional secondary y-axis. No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full - this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Combo design](../../charts/combo/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-021 | Post-0.0.0.5 expansion: histogram as validation infrastructure | Admit `histogram` as the next chart type, targeting release `0.0.0.6`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the binning transform (raw samples to frequency distribution) and the contiguous-bar rendering with numeric linear x-scale are required to validate StoneVerify conformance against the insurance reporting segment's standard loss-distribution and claims-frequency views. Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 7) histogram is a Cartesian sibling that reuses the shared frame and linear x-scale verbatim, adding exactly three net-new generalizations: the binning transform (sqrt-rule default, explicit count/width, pre-binned mode), contiguous bars (zero gap, numeric edge labels with en-dash), and optional overlays (pareto cumulative line on secondary y-axis, bellcurve normal-distribution fit). No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full — this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Histogram design](../../charts/histogram/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-023 | Post-0.0.0.7 expansion: error-bar as validation infrastructure | Admit `error-bar` as the next chart type, targeting release `0.0.0.8`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the whisker-mark primitive (stem + two caps) and the `{y,low,high}` point model extension are required to validate StoneVerify conformance against the insurance reporting segment's standard confidence-interval and percentile-spread views. Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 9) error-bar is a Cartesian sibling that reuses the shared band layout and chrome verbatim, adding exactly two net-new generalizations: the whisker-mark primitive (one vertical stem + two horizontal caps) and the `low`/`high` parallel arrays extending the point model with uncertainty bounds around a center `y`. No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full — this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Error-bar design](../../charts/error-bar/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-024 | Post-0.0.0.8 expansion: arearange as validation infrastructure | Admit `arearange` as the next chart type, targeting release `0.0.0.9`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the pure `{low,high}` point model and the band-fill between two data paths are required to validate StoneVerify conformance against interval and envelope visualizations (percentile spreads, min-max bands, forecast confidence ranges). Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 10) arearange is a Cartesian sibling that reuses line's parity-locked path builder twice (high L→R, low R→L) and the shared frame verbatim. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Arearange design](../../charts/arearange/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-025 | Post-0.0.0.8 expansion: columnrange as validation infrastructure | Admit `columnrange` as the next chart type, targeting release `0.0.0.9`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the floating-bar `{low,high}` mark over discrete categories with a non-zero-anchored value axis is required to validate StoneVerify conformance against range-comparison and band-width views. Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 11) columnrange is a Cartesian sibling that reuses column's band layout and candlestick's floating-bar primitive verbatim, adding no net-new mark. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Columnrange design](../../charts/columnrange/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-026 | Post-0.0.0.9 expansion: waterfall as validation infrastructure | Admit `waterfall` as the next chart type, targeting release `0.0.0.10`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the running-total transform, floating-bar deltas, and connector-lines primitive are required to validate StoneVerify conformance against bridge/waterfall comparison views used in insurance financial reporting and latency-budget breakdowns. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Waterfall design](../../charts/waterfall/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |
| DEC-022 | Post-0.0.0.6 expansion: candlestick as validation infrastructure | Admit `candlestick` as the next chart type, targeting release `0.0.0.7`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the `(o,h,l,c)` point model and the floating-bar primitive (rect between two arbitrary y-values, not baseline-anchored) are required to validate StoneVerify conformance against the insurance reporting segment's standard OHLC summary views (per-interval price, index level, or metric aggregate). Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 8) candlestick is a Cartesian sibling that reuses the shared band layout and chrome verbatim, adding exactly three net-new generalizations: the `(o,h,l,c)` point model (`series[].ohlc`), the floating-bar primitive (wick + body between open and close with doji min-1px rule), and the up/down two-color legend (`upColor`/`downColor` keyed on `close >= open`). Subtypes: candlestick, ohlc, hlc, heikin-ashi, hollow. No other chart type or language is admitted by this decision. Implementation follows the chart admission checklist in full — this decision closes only its Phase 0. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Candlestick design](../../charts/candlestick/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-08 |

| DEC-027 | Post-0.0.0.10 expansion: bullet as validation infrastructure | Admit `bullet` as the next chart type, targeting release `0.0.0.11`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the qualitative-range-band primitive, target-marker tick, and compact KPI layout are required to validate StoneVerify conformance against the insurance reporting segment's standard KPI dashboard views. Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 13) bullet is a Cartesian sibling that rides bar's horizontal orientation and band layout, adding exactly three net-new generalizations: the qualitative-range-band primitive (graded background rects from theme shades), the target-marker tick line, and the `{bulletTarget, bulletRanges}` spec fields. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Bullet design](../../charts/bullet/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-09 |
| DEC-028 | Post-0.0.0.11 expansion: boxplot as validation infrastructure | Admit `boxplot` as the next chart type, targeting release `0.0.0.12`; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the 5-number summary glyph (low, q1, median, q3, high) with optional outlier marks is required to validate StoneVerify conformance against the insurance reporting segment's standard distribution-summary views (claim-amount percentiles, loss-ratio spreads, reserve adequacy ranges). Per the roadmap's own build order (`docs/roadmap/chart-families.md` §3.3 Rank 14) boxplot is a Cartesian sibling that reuses the shared band layout and chrome, adding exactly two net-new generalizations: the `boxData` summary-mode point model (`{low, q1, median, q3, high, outliers?}` per category) and the box-and-whisker mark primitive (IQR rect, median line, whisker stems, cap ticks, outlier circles). Supports vertical and horizontal orientation and grouped multi-series layout. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Boxplot design](../../charts/boxplot/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-09 |
| DEC-029 | Post-0.0.0.12 expansion: lollipop as validation infrastructure | Admit lollipop as the next chart type, targeting release 0.0.0.13; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the stem-and-marker glyph (thin baseline-anchored line capped with a marker head) is required to validate StoneVerify conformance against the insurance reporting segment's ranked KPI and category-comparison views where the low-ink lollipop form reduces visual clutter compared to solid bars. Per the roadmap's own build order lollipop is a Cartesian sibling that reuses column's band layout and line's marker shapes verbatim, adding exactly one net-new mark: the stem-and-head composite (baseline-anchored line + configurable marker symbol). Supports vertical and horizontal orientation and grouped multi-series layout. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Lollipop design](../../charts/lollipop/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-09 |
| DEC-030 | Post-0.0.0.13 expansion: dumbbell as validation infrastructure | Admit dumbbell as the next chart type, targeting release 0.0.0.14; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the connected-dot glyph (two marker heads at low and high values joined by a thin connector) is required to validate StoneVerify conformance against the insurance reporting segment's before-after and min-max comparison views where the gap between two values is the story. Per the roadmap's own build order dumbbell is a Cartesian sibling of lollipop that reuses column's band layout and line's marker shapes verbatim, adding exactly one net-new mark: the connected-dot composite (low marker + high marker + connector line). Uses data for low values and high array for high values with include_zero=False. Supports vertical and horizontal orientation and grouped multi-series layout. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Dumbbell design](../../charts/dumbbell/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-09 |
| DEC-031 | Post-0.0.0.14 expansion: funnel as validation infrastructure | Admit funnel as the next chart type, targeting release 0.0.0.15; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the centered trapezoid-stack glyph with value-to-width scaling is required to validate StoneVerify conformance against the insurance reporting segment's conversion pipeline and drop-off views (quote-to-bind, claims intake, renewal pipeline). Per the roadmap's own build order funnel is the declared substrate exception — no Cartesian axes, own SVG shell — adding exactly three net-new generalizations: the trapezoid-stack mark primitive (centered polygons with per-stage width proportional to value), the `{neckWidth, neckHeight, minWidth}` spec fields, and the `pyramid`/`neck` subtypes. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Funnel design](../../charts/funnel/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-09 |
| DEC-032 | Post-0.0.0.15 expansion: variwide as validation infrastructure | Admit variwide as the next chart type, targeting release 0.0.0.16; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the proportional-width column glyph (each bar's width encodes a second data metric) is required to validate StoneVerify conformance against the insurance reporting segment's weighted-comparison views (premium volume by loss ratio, reserve adequacy by portfolio size). Per the roadmap variwide is a Cartesian sibling that reuses column's rect-mark and y-scale, adding exactly two net-new generalizations: the `series[].widths` per-datum width-metric field and the cumulative-width x-layout strategy (`x_scale="variwide"`) replacing column's equal bands. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Variwide design](../../charts/variwide/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-10 |
| DEC-033 | Post-0.0.0.16 expansion: timeline as validation infrastructure | Admit timeline as the next chart type, targeting release 0.0.0.17; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the event-marker-on-time-axis glyph (markers with leader lines and alternating labels along a datetime spine) is required to validate StoneVerify conformance against the insurance reporting segment's event-sequence views (policy effective dates, claims milestones, renewal timelines). Per the roadmap timeline is a Cartesian sibling that reuses scatter's numeric x-scale and line's marker shapes, adding exactly two net-new generalizations: the `series[].labels` per-event label-text field and the `"numeric"` x-scale strategy with collapsed value axis (no y-gridlines). No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Timeline design](../../charts/timeline/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-10 |
| DEC-034 | Post-0.0.0.17 expansion: windbarb as validation infrastructure | Admit windbarb as the next chart type, targeting release 0.0.0.18; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the meteorological wind-barb glyph (speed-encoded feathers + direction-encoded rotation on a fixed lane along a time axis) is required to validate StoneVerify conformance against the insurance reporting segment's weather-hazard views (station wind observations, forecast meteograms, marine underwriting weather strips). Per the roadmap windbarb is a Cartesian sibling that reuses column's band x-scale and the shared frame's chrome, adding exactly two net-new generalizations: the `series[].direction` per-point wind-direction companion field and the barb-glyph mark (staff + feathers via SVG rotate transform — no trig). No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Windbarb design](../../charts/windbarb/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-10 |
| DEC-035 | Post-0.0.0.18 expansion: streamgraph as validation infrastructure | Admit streamgraph as the next chart type, targeting release 0.0.0.19; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the stacked-ribbon-over-floating-baseline glyph (wiggle or silhouette offset) is required to validate StoneVerify conformance against the insurance reporting segment's temporal volume-composition views (claim category trends, premium source evolution, loss-type flow over policy periods). Per the roadmap streamgraph is a Cartesian sibling of area that reuses line's path builder and area's ribbon fill, adding exactly two net-new generalizations: the `offset` spec field (`"wiggle"` or `"silhouette"` baseline algorithm) and the pre-computed baseline-offset y-domain transform in `build_frame()`. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Streamgraph design](../../charts/streamgraph/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-10 |
| DEC-036 | Post-0.0.0.19 expansion: vector-plot as validation infrastructure | Admit vector-plot as the next chart type, targeting release 0.0.0.20; approved under DEC-017's "explicit approval as necessary validation infrastructure" clause because the arrow-glyph-on-numeric-plane mark (direction-encoded heading + length-encoded magnitude at each x/y anchor) is required to validate StoneVerify conformance against the insurance reporting segment's spatial field views (wind field observations, current vectors, force diagrams for engineering underwriting). Per the roadmap vector-plot is a Cartesian sibling of scatter that reuses scatter's linear x-scale and the shared frame's chrome, adding exactly three net-new generalizations: the `series[].x` per-point numeric x-coordinate companion field, the `series[].length` per-point magnitude companion field, and the arrow-glyph mark (shaft + arrowhead barbs via SVG `<path>` with trigonometric rotation). The `rotationOrigin` and `vectorLength` top-level spec fields control anchor placement and max arrow pixel length. No other chart type or language is admitted by this decision. | [Chart admission checklist](../architecture/chart-admission-checklist.md), [Chart families roadmap](../roadmap/chart-families.md), [Vector-plot design](../../charts/vector-plot/design.md), [Visual integrity strategy](../product/visual-integrity-strategy.md) | 2026-08-10 |

## Open decisions

| Priority | ID | Decision | Current recommendation | Decide before |
|---:|---|---|---|---|

## Discussion order

DEC-005, DEC-006, DEC-008, DEC-009, DEC-010, DEC-011, DEC-012, and DEC-013 are resolved.
The commercial boundary is governed by the approved policy document and must not be inferred from engineering progress.

DEC-005 stakeholder brief: [Compatibility decision brief](decision-briefs/dec-005-compatibility.md).
DEC-017 stakeholder brief: [Visual integrity repositioning brief](decision-briefs/dec-017-visual-integrity.md).
