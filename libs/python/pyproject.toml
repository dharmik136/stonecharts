[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "peakcharts"
version = "0.1.0"
description = "PeakCharts — original, MIT-licensed interactive charting library (Python edition)"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Dharmik Shingala" }]
keywords = ["charts", "svg", "visualization", "plotting"]
dependencies = []

[project.urls]
Homepage = "https://github.com/dharmik136/peakcharts"

[tool.setuptools.packages.find]
where = ["."]
include = ["peakcharts*"]
